<template>
  <div class="hello">
    <h1 class="vui-text-heading--large vui-m-vertical--large">{{ msg }}</h1>
  </div>
</template>

<script>
  export default {
    data () {
      return {
        // note: changing this line won't causes changes
        // with hot-reload because the reloaded component
        // preserves its current state and we are modifying
        // its initial state.
        msg: 'Reach, Sure and Simple'
      }
    }
  }
</script>
